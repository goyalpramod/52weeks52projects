export default function Select ({ options, placeholder, isOpen, selectedValue } : { options: string[], placeholder?: string, isOpen: boolean, selectedValue: string }){
    if (!isOpen) return null;
    
    return (
        <div>

        </div>
    );
}